# Instructions for AI coding agents

This repo contains the code and assetes to build a `marp` presentation about using AI coding agents effectively.

## Environment

- `npm` for package management
- Scripts in `package.json` to build and serve the presentation
